```mermaid
flowchart TD
    U["用户自然语言输入"] --> BE["后端接口"]

    BE --> G["LangGraph 主图<br/>Main Graph"]

    G --> R["意图识别 / Router Agent"]

    R --> C{"意图分类"}

    C -->|"无关内容"| O["无关内容处理节点<br/>返回固定提示语"]
    C -->|"读取飞书"| RA["读取 Agent / Read Agent"]
    C -->|"新增写入"| WA["写入 Agent / Write Agent"]
    C -->|"修改记录"| UA["修改 Agent / Update Agent"]
    C -->|"删除记录"| DA["删除 Agent / Delete Agent"]
    C -->|"意图不清楚"| QA["澄清 Agent / Clarify Agent"]

    RA --> FR["飞书读取工具"]
    WA --> FW["飞书写入工具"]
    UA --> FU["飞书更新工具"]
    DA --> FD["飞书删除工具"]

    FR --> RS["结果整理节点"]
    FW --> RS
    FU --> RS
    FD --> RS
    O --> RS
    QA --> RS

    RS --> BE
    BE --> U