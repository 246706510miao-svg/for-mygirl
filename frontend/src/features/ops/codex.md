# ops 模块入口

`features/ops` 是后台运维界面，不是伴侣互动界面。它负责全量记录查看、详情、追踪、重试和后续重写入能力。

## 接入规则

- API 可以继续兼容当前 `/api/admin/*` 路径，但前端模块命名统一使用 ops。
- 这里只放后台人员能力，不放评论、风格、绑定、积分奖品等用户互动能力。
- 排查记录链路时优先打开追踪面板，再进入后端 `trace` 和 `sync` 模块。
