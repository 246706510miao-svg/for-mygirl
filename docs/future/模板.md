# 功能名称

## 目标

说明这个功能解决什么问题，面向谁使用。

## 接入模块

| 层 | 位置 | 说明 |
|---|---|---|
| 前端主模块 | `frontend/src/features/<module>` | 例如 `record`、`style`、`relationship`、`comment`、`points`、`ops`。 |
| 后端主模块 | `backend/src/main/java/com/formygirl/<module>` | 例如 `record`、`style`、`relationship`、`comment`、`points`、`sync`、`ops`。 |
| 协作模块 |  | 写清楚是否依赖授权、积分、同步、追踪等。 |

## 用户和权限

- 使用者：
- 是否需要绑定用户：
- 对方是否可操作：
- 后台人员是否可操作：

## 主链路

用 5 到 10 行写清楚正常链路，例如：

```text
用户触发
  -> 前端 feature API
  -> 后端 Controller
  -> Service 校验权限
  -> Repository 写表
  -> 返回 DTO
```

## 前端影响

- 页面或组件：
- API helper：
- DTO 类型：
- 状态和错误展示：

## 后端影响

- Controller：
- Service：
- Repository：
- 需要复用的模块：
- 不能耦合的模块：

## 数据库影响

- 新增表：
- 变更表：
- 关键索引：
- 幂等或唯一约束：

## 外部服务影响

- 是否调用 third：
- 是否写飞书：
- 是否需要保留 payload 或错误：
- 是否需要 trace：

## 接口

| 方法 | 路径 | 用途 |
|---|---|---|
|  |  |  |

## 排查入口

- 前端先看：
- 后端先看：
- 数据库先看：
- 关键 ID：

## 需要更新文档

- `docs/接口文档.md`：
- `docs/数据库/codex.md`：
- `backend/codex.md`：
- `frontend/codex.md`：
- 其他：
